spring:
  application:
    name: FEBS-Admin
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8001
        group: DEFAULT_GROUP
        prefix: febs-admin
        file-extension: yaml

logging:
  level:
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error